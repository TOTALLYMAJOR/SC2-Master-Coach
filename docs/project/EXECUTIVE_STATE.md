# Executive State

## What This Is

SC2 Master Coach is a local-first Windows coaching application for StarCraft II. Its checked-in product combines offline replay intelligence, a manual-report live Combat HUD, deterministic strategic policy, and a bounded Python advisory runtime.

## North Star

A player installs the app, supplies a real replay or explicit battlefield report, understands the evidence and uncertainty behind one useful next decision, and improves without surrendering private game data or relying on a developer.

## Current Reality

Seventeen material repository capabilities are `TESTED` locally. Master Intel is the executable default at `/`; the Combat HUD remains available at `/hud`. Replay cases and player packs persist locally. The Strategic OS owns live state. The Python Digital Twin is advisory and limited to a bounded PvT three-base scope. Comparison correctly withholds calculations that lack normalized evidence. The evidence graph and dependency register now organize proof and sequencing claims without promoting target-environment or user-outcome proof.

## Capability State

- TESTED: 17
- VERIFIED in a target Windows environment: 0
- DEPLOYED with current provider evidence: 0
- USED with customer evidence: 0
- COMMERCIALLY_PROVEN: 0

Highest-value tested capabilities are replay ingestion and persistence, the evidence-bounded Combat HUD, Strategic OS, the comparison guard, and the Python shadow Digital Twin.

## What Is Not Proven

- Clean-Windows install and real-replay completion from a current artifact.
- Real frame capture and microphone behavior on the target machine.
- Expert validity or player-outcome value of strategy benchmarks.
- Current deployment, active usage, retention, measured improvement, or revenue.
- The intended long-term product center: Master Intel or Combat HUD.

## Primary Journey

The executable primary journey is local launch → replay import → persisted case → evidence review → provisional practice focus. It is locally `TESTED`; target-environment and human acceptance are `UNVERIFIED`.

## Critical Decisions

- Strategic OS remains the only live state authority; Python is advisory.
- Runtime remains local, offline-first, and loopback-only.
- Master Intel is the executable default, but that product-positioning decision needs owner confirmation.
- `.project/state.json` is the machine ledger; human registers are evidence views.
- The evidence graph and dependency register govern proof sequencing and graph references inside the project-state control plane.

## Critical Blockers

- P1: no current clean-Windows real-replay acceptance receipt.
- P1: normalized replay fingerprints and replay-to-master calculations are absent.
- P2: strategy benchmarks and coaching outcomes lack expert/player validation.
- P2: deployment, usage, retention, outcome, and revenue evidence are absent.
- P2: primary product authority is inconsistent across code and documentation.

## Next Proof Event

`PROOF-NEXT-001`: install a current exact-commit Windows artifact on a clean machine, import a real supported replay, persist and review the case, exercise frame/voice behavior or their explicit fallbacks, and obtain a human comprehension record.

## Next Actions

1. Confirm the primary product journey.
2. Produce and checksum a current Windows artifact.
3. Execute the clean-Windows real-replay journey.
4. Record human acceptance and reconcile the evidence.
5. Select the next bounded product slice from what the proof event reveals.

## Commercial / Operational Evidence

Release workflow definitions and version tags exist. No current provider receipt, clean-install record, consented usage, retention, outcome, or revenue evidence was inspected. Commercial readiness is `UNVERIFIED`.

## Confidence

**MEDIUM.** Repository architecture, source boundaries, and local tests are strong. Confidence stops below `VERIFIED` because the target Windows environment, real users, and external systems were not observed.
